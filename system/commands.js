// Command/undo-redo boundary.
// The source-driven editor currently uses snapshot-based history with command-like actions.
// This module is intentionally separate so full command classes can be dropped in later.
